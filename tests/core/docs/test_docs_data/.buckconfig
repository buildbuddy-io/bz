[buildfile]
name=TARGETS.fixture

[project]
ignore=ignored

[repositories]
root = .
cell = cell
prelude = prelude
buck = buck
config = config
toolchains = toolchains
special = special
prelude = prelude
