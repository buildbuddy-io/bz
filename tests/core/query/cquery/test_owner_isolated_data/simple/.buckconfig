[buildfile]
name=TARGETS.fixture

[project]
ignore=ignored,bin/ignored,bin/ignored.txt
package_boundary_exceptions=.

[repositories]
root = .
buck = buck
toolchains = toolchains
special = special
nano_prelude = nano_prelude
config = config

[cell_aliases]
prelude = nano_prelude

[external_cells]
nano_prelude = bundled
