load("@prelude//utils:source_listing.bzl", "source_listing")

oncall("apple_build_infra")

source_listing()

python_bootstrap_binary(
    name = "build_index_store",
    main = "main.py",
    visibility = ["PUBLIC"],
    deps = [
        "prelude//apple/tools/index:merge_index_store_lib",
    ],
)
